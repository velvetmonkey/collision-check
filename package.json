{
  "private": true,
  "name": "collision-check",
  "version": "0.1.0",
  "description": "Witness refinement analyzer + minimal receipt compiler: do these observations carry enough information to justify this claim?",
  "type": "module",
  "license": "Apache-2.0",
  "bin": { "collision-check": "bin/collision-check" },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "engines": { "node": ">=18" }
}
